<main class="container mt-3">
  <div class="row row-cols-1">
    <% @users.each do |user| %>
      <%= link_to render('/user_card', user:), user_path(user.id) %>
    <% end %>
  </div class="row">
</main>
